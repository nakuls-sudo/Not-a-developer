<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Chef's Portal</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{
  --ink:#1a1410;--cream:#faf7f2;--warm:#f5ede0;--parchment:#ede4d4;
  --sage:#5c7a5e;--rust:#b85c38;--gold:#c9973a;--mist:#8fa8a0;
  --ink2:#3d3530;--subtle:#a09080;
}
html,body{height:100%;overflow:hidden;background:var(--cream);color:var(--ink);font-family:'DM Sans',sans-serif}

/* ── SPLASH ── */
#splash{
  position:fixed;inset:0;z-index:100;
  background:var(--ink);display:flex;flex-direction:column;
  justify-content:center;align-items:center;
  transition:opacity 0.8s ease,visibility 0.8s ease;
}
#splash.gone{opacity:0;visibility:hidden;pointer-events:none}
.splash-bg{
  position:absolute;inset:0;
  background:radial-gradient(ellipse 80% 60% at 50% 110%, #3d2a1a 0%, var(--ink) 65%);
}
.splash-ring{
  position:absolute;width:500px;height:500px;border-radius:50%;
  border:1px solid rgba(201,151,58,0.15);
  animation:pulse 3s ease-in-out infinite;
}
.splash-ring:nth-child(2){width:360px;height:360px;animation-delay:0.5s;border-color:rgba(201,151,58,0.25)}
.splash-ring:nth-child(3){width:220px;height:220px;animation-delay:1s;border-color:rgba(201,151,58,0.4)}
@keyframes pulse{0%,100%{transform:scale(1);opacity:0.6}50%{transform:scale(1.04);opacity:1}}
.splash-content{position:relative;text-align:center}
.splash-eyebrow{font-size:11px;letter-spacing:4px;text-transform:uppercase;color:var(--gold);margin-bottom:18px;opacity:0;animation:rise 0.8s 0.3s forwards}
.splash-title{font-family:'Playfair Display',serif;font-size:clamp(3.5rem,8vw,6rem);color:var(--cream);line-height:1;margin-bottom:10px;opacity:0;animation:rise 0.8s 0.5s forwards}
.splash-title em{color:var(--gold);font-style:italic}
.splash-sub{font-size:15px;color:rgba(250,247,242,0.45);margin-bottom:48px;font-weight:300;opacity:0;animation:rise 0.8s 0.7s forwards}
.splash-btn{
  background:var(--gold);color:var(--ink);border:none;
  padding:14px 44px;border-radius:2px;font-size:14px;font-weight:500;
  letter-spacing:2px;text-transform:uppercase;cursor:pointer;
  opacity:0;animation:rise 0.8s 0.9s forwards;
  transition:background 0.2s,transform 0.15s;
}
.splash-btn:hover{background:#d4a445;transform:translateY(-1px)}
@keyframes rise{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}

/* ── NOTIFICATION PILLS ── */
.notif-tray{
  position:absolute;top:36px;right:36px;
  display:flex;flex-direction:column;gap:10px;align-items:flex-end;
  opacity:0;animation:rise 0.6s 1.2s forwards;
}
.notif-pill{
  display:flex;align-items:center;gap:10px;
  background:rgba(250,247,242,0.06);backdrop-filter:blur(12px);
  border:1px solid rgba(250,247,242,0.12);border-radius:40px;
  padding:10px 16px;
  animation:slideLeft 0.5s ease-out both;
}
.notif-dot{width:7px;height:7px;border-radius:50%;flex-shrink:0}
.dot-gold{background:var(--gold)}
.dot-sage{background:#7ab87d}
.notif-pill-text{font-size:12px;color:rgba(250,247,242,0.7)}
.notif-pill-time{font-size:11px;color:rgba(250,247,242,0.35);margin-left:4px}
@keyframes slideLeft{from{opacity:0;transform:translateX(30px)}to{opacity:1;transform:translateX(0)}}

/* ── MAIN APP ── */
#app{
  height:100vh;display:grid;
  grid-template-columns:300px 1fr 320px;
  grid-template-rows:56px 1fr;
  opacity:0;transition:opacity 0.6s ease;pointer-events:none;
}
#app.visible{opacity:1;pointer-events:all}

/* Top bar */
.topbar{
  grid-column:1/-1;
  display:flex;align-items:center;justify-content:space-between;
  padding:0 28px;border-bottom:1px solid var(--parchment);
  background:var(--cream);
}
.topbar-brand{font-family:'Playfair Display',serif;font-size:18px;letter-spacing:-0.5px}
.topbar-brand em{color:var(--rust);font-style:italic}
.topbar-meta{font-size:12px;color:var(--subtle);letter-spacing:1px;text-transform:uppercase}
.topbar-clear{
  font-size:12px;color:var(--subtle);background:none;border:1px solid var(--parchment);
  padding:5px 14px;border-radius:2px;cursor:pointer;letter-spacing:1px;text-transform:uppercase;
  transition:border-color 0.2s,color 0.2s;
}
.topbar-clear:hover{border-color:var(--rust);color:var(--rust)}

/* Panels */
.col{height:calc(100vh - 56px);overflow-y:auto;padding:24px}
.col::-webkit-scrollbar{width:3px}
.col::-webkit-scrollbar-thumb{background:var(--parchment);border-radius:2px}

/* LEFT — pantry */
#col-pantry{background:var(--cream);border-right:1px solid var(--parchment)}
.col-heading{
  font-size:10px;letter-spacing:3px;text-transform:uppercase;
  color:var(--subtle);margin-bottom:16px;
}
.ingredient-grid{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:28px}
.chip{
  padding:6px 13px;border:1px solid var(--parchment);border-radius:2px;
  font-size:12px;cursor:pointer;color:var(--ink2);background:var(--cream);
  transition:all 0.15s;user-select:none;
}
.chip:hover{border-color:var(--mist)}
.chip.sel{background:var(--ink);color:var(--cream);border-color:var(--ink)}
.section-rule{border:none;border-top:1px solid var(--parchment);margin:4px 0 20px}

/* Meal suggestions */
.suggestion-list{display:flex;flex-direction:column;gap:6px}
.s-card{
  padding:12px 14px;border:1px solid var(--parchment);border-radius:3px;
  cursor:pointer;transition:all 0.15s;background:var(--cream);
}
.s-card:hover{border-color:var(--mist);background:var(--warm)}
.s-card.active{border-color:var(--rust);background:var(--warm)}
.s-card.ready-card{border-left:3px solid var(--sage)}
.s-top{display:flex;justify-content:space-between;align-items:baseline;gap:8px}
.s-name{font-size:13px;font-weight:500;color:var(--ink)}
.s-cuisine{font-size:10px;color:var(--subtle);letter-spacing:1px;text-transform:uppercase}
.s-miss{font-size:11px;color:var(--gold);margin-top:3px}
.s-ready{font-size:11px;color:var(--sage);margin-top:3px}
.empty-state{font-size:13px;color:var(--subtle);padding:12px 0;line-height:1.6}

/* MIDDLE — recipe view */
#col-recipe{background:var(--warm);border-right:1px solid var(--parchment)}
.hero-img{
  width:100%;height:260px;object-fit:cover;
  border-radius:2px;margin-bottom:24px;display:block;
  background-size:cover;background-position:center;
}
.recipe-title{
  font-family:'Playfair Display',serif;font-size:28px;
  line-height:1.1;margin-bottom:6px;
}
.recipe-cuisine{font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--subtle);margin-bottom:24px}
.recipe-cols{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.rcol-head{font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--subtle);margin-bottom:12px}
.ing-row{
  display:flex;align-items:center;gap:8px;
  padding:7px 0;border-bottom:1px solid rgba(26,20,16,0.07);
  font-size:13px;
}
.ing-row:last-child{border-bottom:none}
.ing-check{width:16px;height:16px;border-radius:50%;border:1.5px solid;flex-shrink:0;display:flex;align-items:center;justify-content:center}
.ing-have{border-color:var(--sage);background:rgba(92,122,94,0.12);color:var(--sage)}
.ing-miss{border-color:var(--parchment);color:var(--parchment)}
.step-row{margin-bottom:12px;display:flex;gap:12px}
.step-num{
  font-family:'Playfair Display',serif;font-size:22px;color:var(--parchment);
  line-height:1;flex-shrink:0;width:24px;text-align:right;
}
.step-text{font-size:13px;line-height:1.6;color:var(--ink2);padding-top:3px}
.recipe-placeholder{
  display:flex;flex-direction:column;justify-content:center;align-items:center;
  height:100%;text-align:center;padding:40px;
}
.placeholder-icon{font-size:48px;margin-bottom:16px;opacity:0.15}
.placeholder-text{font-size:14px;color:var(--subtle);line-height:1.7}
.done-btn{
  margin-top:28px;width:100%;padding:13px;
  background:var(--ink);color:var(--cream);border:none;
  font-size:12px;letter-spacing:2px;text-transform:uppercase;
  cursor:pointer;border-radius:2px;transition:background 0.2s;
  display:none;
}
.done-btn:hover{background:var(--rust)}

/* RIGHT — reminders */
#col-remind{background:var(--ink);color:var(--cream)}
#col-remind .col-heading{color:rgba(250,247,242,0.35)}
.remind-card{
  padding:14px 16px;border:1px solid rgba(250,247,242,0.08);
  border-radius:3px;margin-bottom:8px;
  transition:background 0.15s;cursor:default;
}
.remind-card.due{border-left:3px solid var(--gold);background:rgba(201,151,58,0.06)}
.remind-label{font-size:10px;letter-spacing:2px;text-transform:uppercase;margin-bottom:4px}
.lbl-med{color:var(--gold)}
.lbl-task{color:var(--mist)}
.remind-name{font-size:14px;font-weight:500;color:var(--cream)}
.remind-time{font-size:12px;color:rgba(250,247,242,0.35);margin-top:2px}
.mindful-card{
  margin-top:20px;padding:18px 16px;
  background:rgba(250,247,242,0.04);
  border-radius:3px;border:1px solid rgba(250,247,242,0.08);
}
.mindful-q{font-family:'Playfair Display',serif;font-style:italic;font-size:15px;color:rgba(250,247,242,0.7);line-height:1.5}

/* ── EXERCISE OVERLAY ── */
#exercise-overlay{
  position:fixed;inset:0;z-index:200;
  background:var(--ink);display:none;
  flex-direction:column;justify-content:center;align-items:center;
  padding:40px;text-align:center;
}
#exercise-overlay.show{display:flex;animation:fadeUp 0.6s ease-out}
@keyframes fadeUp{from{opacity:0;transform:translateY(30px)}to{opacity:1;transform:translateY(0)}}
.ex-eyebrow{font-size:10px;letter-spacing:4px;text-transform:uppercase;color:var(--rust);margin-bottom:16px}
.ex-title{font-family:'Playfair Display',serif;font-size:clamp(2rem,5vw,3.5rem);color:var(--cream);margin-bottom:8px}
.ex-sub{font-size:14px;color:rgba(250,247,242,0.4);margin-bottom:36px}
.ex-card{
  background:rgba(250,247,242,0.04);border:1px solid rgba(250,247,242,0.12);
  border-radius:4px;max-width:380px;width:100%;overflow:hidden;
}
.ex-img{width:100%;height:240px;object-fit:cover;background-size:cover;background-position:center}
.ex-body{padding:20px}
.ex-pose{font-family:'Playfair Display',serif;font-size:22px;color:var(--cream);margin-bottom:8px}
.ex-desc{font-size:13px;color:rgba(250,247,242,0.5);line-height:1.6}
.ex-back{
  margin-top:28px;background:none;border:1px solid rgba(250,247,242,0.2);
  color:rgba(250,247,242,0.6);padding:12px 36px;border-radius:2px;
  font-size:12px;letter-spacing:2px;text-transform:uppercase;cursor:pointer;
  transition:border-color 0.2s,color 0.2s;
}
.ex-back:hover{border-color:var(--cream);color:var(--cream)}
</style>
</head>
<body>

<!-- SPLASH -->
<div id="splash">
  <div class="splash-bg"></div>
  <div class="splash-ring"></div>
  <div class="splash-ring"></div>
  <div class="splash-ring"></div>
  <div class="notif-tray" id="notif-tray"></div>
  <div class="splash-content">
    <p class="splash-eyebrow">Your daily kitchen companion</p>
    <h1 class="splash-title">Taste<br><em>Genie</em></h1>
    <p class="splash-sub">Get Ingredients, We've Got Ideas</p>
    <button class="splash-btn" onclick="enterApp()">Open Pantry</button>
  </div>
</div>

<!-- MAIN APP -->
<div id="app">
  <!-- Topbar -->
  <header class="topbar">
    <span class="topbar-brand">Chef's <em>Portal</em></span>
    <span class="topbar-meta" id="topbar-date"></span>
    <button class="topbar-clear" onclick="clearAll()">Clear pantry</button>
  </header>

  <!-- Left: Pantry -->
  <div class="col" id="col-pantry">
    <p class="col-heading">Pantry ingredients</p>
    <div class="ingredient-grid" id="ingredient-grid"></div>
    <hr class="section-rule">
    <p class="col-heading">Top suggestions</p>
    <div class="suggestion-list" id="suggestion-list">
      <p class="empty-state">Select ingredients from your pantry to see what you can cook.</p>
    </div>
  </div>

  <!-- Middle: Recipe -->
  <div class="col" id="col-recipe">
    <div id="recipe-view">
      <div class="recipe-placeholder">
        <div class="placeholder-icon">&#127859;</div>
        <p class="placeholder-text">Choose a meal from the suggestions<br>to see the full recipe and ingredient checklist.</p>
      </div>
    </div>
  </div>

  <!-- Right: Reminders -->
  <div class="col" id="col-remind">
    <p class="col-heading">Today's reminders</p>
    <div id="reminder-list"></div>
    <div class="mindful-card">
      <p class="mindful-q" id="mindful-quote"></p>
    </div>
  </div>
</div>

<!-- Exercise Overlay -->
<div id="exercise-overlay">
  <p class="ex-title">Nice work — now move your body</p>
  <h2 class="ex-eyebrow">Time to Exercise</h2>
  <p class="ex-sub">A quick pose to close the loop on your meal.</p>
  <div class="ex-card">
    <div class="ex-img" id="ex-img"></div>
    <div class="ex-body">
      <p class="ex-pose" id="ex-pose"></p>
      <p class="ex-desc" id="ex-desc"></p>
    </div>
  </div>
  <button class="ex-back" onclick="location.reload()">Back to pantry</button>
</div>

<script>
const MEDS = [
  {name:"Folic Acid",times:["08:00","16:00"]},
  {name:"Calcium Supplement",times:["12:00"]},
  {name:"Zinc Supplement",times:["18:30"]},
  {name:"Metformin T2",times:["08:00","16:00","00:00"]},
  {name:"Nepromine Supplement",times:["12:00"]},
  {name:"D-Door B2",times:["18:30"]}
];
const TASKS = ["Check your weight","Check blood sugar","Drink a glass of water","Take a deep breath","Smile is the best therapy", "Love yourself"];
const INGREDIENTS = ["Rice","Wheat Flour","Eggs","Chicken","Potatoes","Tomatoes","Onions","Garlic","Ginger","Green Chilies","Bell Peppers","Cabbage","Milk","Butter","Cheese","Cooking Oil","Spices"];
const MEALS = [
  {name:"Chicken Curry with Rice",type:"Indian",ingredients:["Chicken","Rice","Tomatoes","Onions","Garlic","Ginger","Spices"],steps:["Sauté onions, ginger, and garlic until fragrant.","Add spices and tomatoes, cook down into a thick base.","Add chicken and simmer on low for 20 minutes.","Serve over steamed rice."],img:"https://images.unsplash.com/photo-1603894584373-5ac82b2ae398?auto=format&fit=crop&w=800&q=80"},
  {name:"Aloo Paratha",type:"Indian",ingredients:["Wheat Flour","Potatoes","Spices"],steps:["Boil and mash potatoes with spices and herbs.","Stuff the filling into portioned wheat dough balls.","Roll out and cook on a hot tawa with butter until golden."],img:"https://images.pexels.com/photos/33428723/pexels-photo-33428723.jpeg"},
  {name:"Egg Bhurji",type:"Indian",ingredients:["Eggs","Onions","Tomatoes","Green Chilies","Spices"],steps:["Sauté onions, tomatoes, and chilies until soft.","Crack in eggs and scramble over medium heat.","Season generously and serve hot."],img:"https://images.pexels.com/photos/13599631/pexels-photo-13599631.jpeg"},
  {name:"Nasi Goreng",type:"Indonesian",ingredients:["Rice","Eggs","Chicken","Garlic","Onions","Green Chilies"],steps:["Stir-fry garlic, onions, and chilies in hot oil.","Add chicken and cook through, then fold in rice.","Push rice aside, fry an egg, and combine."],img:"https://images.pexels.com/photos/13425806/pexels-photo-13425806.jpeg"},
  {name:"Mie Goreng",type:"Indonesian",ingredients:["Wheat Flour","Eggs","Chicken","Cabbage"],steps:["Boil noodles until just tender, drain well.","Stir-fry chicken and cabbage in a hot wok.","Add noodles and toss with sauces until coated."],img:"https://images.pexels.com/photos/8395783/pexels-photo-8395783.jpeg"},
  {name:"Chicken Tacos",type:"Mexican",ingredients:["Wheat Flour","Chicken","Onions","Bell Peppers"],steps:["Sear chicken strips with bell peppers and onions.","Warm flour tortillas in a dry pan.","Assemble with toppings of your choice."],img:"https://images.pexels.com/photos/36498704/pexels-photo-36498704.jpeg"},
  {name:"Mashed Potatoes",type:"American",ingredients:["Potatoes","Butter","Milk"],steps:["Boil potatoes until fork-tender.","Drain and mash with warm butter and milk.","Season to taste and serve immediately."],img:"https://images.pexels.com/photos/30635679/pexels-photo-30635679.jpeg"},
  {name:"Omelette",type:"American",ingredients:["Eggs","Cheese","Bell Peppers","Onions"],steps:["Whisk eggs with a pinch of salt.","Sauté bell peppers and onions until softened.","Pour eggs over fillings, fold, and top with cheese."],img:"https://images.pexels.com/photos/23884204/pexels-photo-23884204.jpeg"}
];
const YOGA = [
  {name:"Vrikshasana",desc:"Balance on one foot to strengthen hips and improve focus.",img:"https://images.pexels.com/photos/5439729/pexels-photo-5439729.jpeg"},
  {name:"Virabhadrasana",desc:"Open stance that builds leg strength and stamina.",img:"https://images.pexels.com/photos/18808528/pexels-photo-18808528.jpeg"},
  {name:"Bhujangasana",desc:"Gentle backbend that stretches the chest and spine.",img:"https://images.unsplash.com/photo-1599447421416-3414500d18a5?w=600"},
  {name:"Balasana",desc:"A calming resting stretch for back, hips, and shoulders.",img:"https://images.pexels.com/photos/9194293/pexels-photo-9194293.jpeg"},
  {name:"Trikonasana",desc:"Full-body stretch that improves spinal flexibility.",img:"https://images.unsplash.com/photo-1575052814086-f385e2e2ad1b?w=600"}
];

let selected = new Set();
let activeCard = null;

function initNotifs() {
  const tray = document.getElementById('notif-tray');
  const now = new Date();
  const cur = now.getHours()*60 + now.getMinutes();
  let i = 0;
  MEDS.forEach(med => {
    med.times.forEach(t => {
      const [h,m] = t.split(':').map(Number);
      if (Math.abs(cur - (h*60+m)) <= 90) {
        const el = document.createElement('div');
        el.className = 'notif-pill';
        el.style.animationDelay = (i*0.15)+'s';
        el.innerHTML = `<span class="notif-dot dot-gold"></span><span class="notif-pill-text">${med.name}</span><span class="notif-pill-time">${t}</span>`;
        tray.appendChild(el); i++;
      }
    });
  });
  const task = TASKS[Math.floor(Math.random()*TASKS.length)];
  const el = document.createElement('div');
  el.className = 'notif-pill'; el.style.animationDelay = (i*0.15)+'s';
  el.innerHTML = `<span class="notif-dot dot-sage"></span><span class="notif-pill-text">${task}</span>`;
  tray.appendChild(el);
}

function initTopbar() {
  const d = new Date();
  document.getElementById('topbar-date').textContent = d.toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'long'});
}

function initReminders() {
  const list = document.getElementById('reminder-list');
  const now = new Date();
  const cur = now.getHours()*60 + now.getMinutes();
  MEDS.forEach(med => {
    med.times.forEach(t => {
      const [h,m] = t.split(':').map(Number);
      const due = Math.abs(cur-(h*60+m)) <= 90;
      const div = document.createElement('div');
      div.className = 'remind-card'+(due?' due':'');
      div.innerHTML = `<p class="remind-label lbl-med">Medicine</p><p class="remind-name">${med.name}</p><p class="remind-time">${t}</p>`;
      list.appendChild(div);
    });
  });
  const task = TASKS[Math.floor(Math.random()*TASKS.length)];
  const mindful = ['Stay hydrated — drink water before your next meal.','Sit up straight and take three slow breaths.','A short walk after eating aids digestion.','Rest when you need to — your body knows best.'];
  document.getElementById('mindful-quote').textContent = mindful[Math.floor(Math.random()*mindful.length)];
}

function initChips() {
  const grid = document.getElementById('ingredient-grid');
  [...INGREDIENTS].sort().forEach(ing => {
    const el = document.createElement('span');
    el.className = 'chip'; el.textContent = ing;
    el.onclick = () => {
      selected.has(ing) ? selected.delete(ing) : selected.add(ing);
      el.classList.toggle('sel');
      updateSuggestions();
    };
    grid.appendChild(el);
  });
}

function updateSuggestions() {
  const list = document.getElementById('suggestion-list');
  if (selected.size === 0) {
    list.innerHTML = '<p class="empty-state">Select ingredients from your pantry to see what you can cook.</p>';
    return;
  }
  const scored = MEALS.map(m => ({...m, miss: m.ingredients.filter(i => !selected.has(i))})).sort((a,b)=>a.miss.length-b.miss.length);
  list.innerHTML = '';
  scored.slice(0,7).forEach(m => {
    const el = document.createElement('div');
    el.className = 's-card'+(m.miss.length===0?' ready-card':'');
    el.innerHTML = `<div class="s-top"><span class="s-name">${m.name}</span><span class="s-cuisine">${m.type}</span></div>`
      + (m.miss.length===0 ? '<p class="s-ready">All ingredients available</p>' : `<p class="s-miss">Need: ${m.miss.join(', ')}</p>`);
    el.onclick = () => { document.querySelectorAll('.s-card').forEach(c=>c.classList.remove('active')); el.classList.add('active'); showRecipe(m); };
    list.appendChild(el);
  });
}

function showRecipe(meal) {
  const view = document.getElementById('recipe-view');
  const ings = meal.ingredients.map(i =>
    `<div class="ing-row"><span class="ing-check ${selected.has(i)?'ing-have':'ing-miss'}">${selected.has(i)?'✓':'—'}</span>${i}</div>`
  ).join('');
  const steps = meal.steps.map((s,idx) =>
    `<div class="step-row"><span class="step-num">${idx+1}</span><span class="step-text">${s}</span></div>`
  ).join('');
  view.innerHTML = `
    <div class="hero-img" style="background-image:url('${meal.img}')"></div>
    <h2 class="recipe-title">${meal.name}</h2>
    <p class="recipe-cuisine">${meal.type}</p>
    <div class="recipe-cols">
      <div><p class="rcol-head">Ingredients</p>${ings}</div>
      <div><p class="rcol-head">Steps</p>${steps}</div>
    </div>
    <button class="done-btn" onclick="finishMeal()" style="display:block">I'm done eating ↗</button>`;
}

function finishMeal() {
  const pose = YOGA[Math.floor(Math.random()*YOGA.length)];
  document.getElementById('ex-pose').textContent = pose.name;
  document.getElementById('ex-desc').textContent = pose.desc;
  document.getElementById('ex-img').style.backgroundImage = `url('${pose.img}')`;
  document.getElementById('exercise-overlay').classList.add('show');
}

function clearAll() {
  selected.clear();
  document.querySelectorAll('.chip').forEach(c=>c.classList.remove('sel'));
  updateSuggestions();
  document.getElementById('recipe-view').innerHTML = `<div class="recipe-placeholder"><div class="placeholder-icon">&#127859;</div><p class="placeholder-text">Choose a meal from the suggestions<br>to see the full recipe and ingredient checklist.</p></div>`;
}

function enterApp() {
  document.getElementById('splash').classList.add('gone');
  const app = document.getElementById('app');
  app.style.display = 'grid';
  setTimeout(()=>app.classList.add('visible'),50);
}

window.onload = () => { initNotifs(); initTopbar(); initReminders(); initChips(); };
</script>
</body>
</html>
